# 购物商城系统 - 从零启动指南

> 适用场景：电脑开机 / WSL 重启后，重新启动项目的完整操作步骤
> 项目位置 (WSL): `~/Project/shopping-system/`
> 数据库: Docker MySQL（端口 3307）
> 网络模式: WSL 镜像网络模式（Windows 和 WSL localhost 互通）

---

## 目录

- [一、两种启动方式对比](#一两种启动方式对比)
- [二、方式 A：Docker MySQL + 本地开发（推荐）](#二方式-adocker-mysql--本地开发推荐)
- [三、方式 B：Docker 全套部署（演示/生产）](#三方式-bdocker-全套部署演示生产)
- [四、WSL 镜像网络模式说明](#四wsl-镜像网络模式说明)
- [五、代码更新后同步到 Docker](#五代码更新后同步到-docker)
- [六、常见问题排查](#六常见问题排查)
- [七、项目信息速查](#七项目信息速查)

---

## 一、两种启动方式对比

| 方式 | 数据库 | 后端 | 前端 | 适用场景 | 推荐度 |
|------|--------|------|------|---------|--------|
| **A. Docker MySQL + 本地开发** | Docker | IDEA / Maven | Vite | 日常开发（有热更新） | ⭐⭐⭐⭐⭐ |
| **B. Docker 全套部署** | Docker | Docker 容器 | Docker + Nginx | 演示、部署到服务器 | ⭐⭐⭐⭐ |

---

## 二、方式 A：Docker MySQL + 本地开发（推荐）

MySQL 跑在 Docker 里，后端用 IDEA 或 Maven 跑，前端用 Vite 跑。既有统一的数据库环境，又有热更新的开发体验。

### 开机启动 4 步

```
1. 启动 Docker Desktop
2. 打开 WSL，启动 Docker MySQL
3. 启动后端（IDEA 或 Maven）
4. 启动前端（Vite）
5. 浏览器访问 http://localhost:5173
```

### 详细步骤

#### 第 1 步：启动 Docker Desktop

任务栏右下角找到 Docker 鲸鱼图标，确保它在运行。

#### 第 2 步：启动 Docker MySQL

打开 WSL：
```powershell
wsl -d Ubuntu
```

只启动 MySQL 容器：
```bash
cd ~/Project/shopping-system
docker compose up -d mysql
```

确认状态：
```bash
docker compose ps mysql
# STATUS 应该是 Up (healthy)
```

#### 第 3 步：启动后端

后端连 Docker MySQL（端口 3307），`application.yml` 的 mysql profile 配置：
```yaml
url: jdbc:mysql://localhost:3307/shopping?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

启动后端（IDEA 里直接运行 `ShoppingApplication`，或终端执行）：

```bash
cd ~/Project/shopping-system/backend
mvn spring-boot:run -s ../maven-settings.xml
```

看到「后端启动成功」就 OK。

#### 第 4 步：启动前端

新开一个 WSL 终端：
```bash
cd ~/Project/shopping-system/frontend
npm run dev
```

#### 第 5 步：访问

浏览器打开 `http://localhost:5173`，用 `admin/123456` 登录。

---

## 三、方式 B：Docker 全套部署（演示/生产）

前后端 + 数据库全部跑在 Docker 里，一条命令启动。适合演示给别人看，或者部署到服务器。

### 一键启动

```bash
cd ~/Project/shopping-system
docker compose up -d
```

> 第一次启动需要构建镜像（5-15 分钟），之后启动只要几秒。

### 访问

| 服务 | 地址 |
|------|------|
| 前端页面 | `http://localhost`（80 端口） |
| 后端 API | `http://localhost:8081` |
| MySQL | `localhost:3307`（Navicat 连接用） |

### 常用命令

```bash
# 查看状态
docker compose ps

# 查看日志
docker compose logs -f           # 所有服务
docker compose logs -f backend   # 只看后端
docker compose logs -f mysql     # 只看数据库
docker compose logs -f frontend  # 只看前端

# 停止
docker compose stop

# 停止并删除容器（数据保留在 volume 里）
docker compose down

# 停止并删除所有（包括数据，慎用！）
docker compose down -v

# 重启某个服务
docker compose restart backend
```

### Navicat 连 Docker MySQL

| 字段 | 值 |
|------|-----|
| 主机 | `localhost` |
| 端口 | `3307` |
| 用户名 | `root` |
| 密码 | `123456` |

---

## 四、WSL 镜像网络模式说明

本项目已开启 **WSL 镜像网络模式（mirrored networking）**，Windows 和 WSL 的 `localhost` 完全互通。

### 当前配置

- `.wslconfig` 位置：`C:\Users\42969\.wslconfig`
- 内容：
  ```ini
  [wsl2]
  networkingMode=mirrored
  dnsTunneling=true
  firewall=true
  autoProxy=true
  ```
- 效果：WSL 里的 `localhost` = Windows 的 `localhost`

### 验证镜像模式是否生效

```bash
# 在 WSL 里执行，能连得上说明镜像模式生效
mysql -h 127.0.0.1 -P 3307 -u root -p123456 -e "SELECT 1;"
```

### 镜像模式出问题怎么回退

删掉 `C:\Users\42969\.wslconfig`，然后 `wsl --shutdown` 重启 WSL。

---

## 五、代码更新后同步到 Docker

日常开发用 Vite + IDEA（方式 A），有热更新，不需要重新构建 Docker。

需要更新 Docker 部署时：

### 后端代码更新了

```bash
docker compose up -d --build backend
```

### 前端代码更新了

```bash
docker compose up -d --build frontend
```

### 前后端都更新了

```bash
docker compose up -d --build
```

### 只改了数据库相关的

如果修改了 `schema-mysql.sql`，需要重新初始化数据库（会清空数据）：

```bash
docker compose down -v     # 删除容器和数据卷
docker compose up -d       # 重新启动（会自动重新建表）
```

---

## 六、常见问题排查

### Q1: 后端启动报错「连不上 MySQL」

1. Docker Desktop 启动了吗？
2. MySQL 容器在跑吗？ → `docker compose ps mysql`
3. 手动测试连接 → `mysql -h 127.0.0.1 -P 3307 -u root -p123456 -e "SELECT 1;"`
4. 确认 `application.yml` 里端口是 `3307`
5. 确认 profile 是 `mysql`

### Q2: 浏览器打不开前端页面

- 确认前端在运行 → `ps aux | grep vite`
- 镜像模式下直接访问 `http://localhost:5173`
- 如果不行，检查 `vite.config.js` 里有 `host: '0.0.0.0'`

### Q3: 前端页面能打开但接口报错

- 确认后端在运行 → `curl http://localhost:8080/api/products/list`
- 检查 Vite 代理 → `vite.config.js` 里的 target 端口对不对
- 浏览器 F12 看 Network 面板

### Q4: 登录失败，密码不对

- 确认连的是 Docker MySQL（端口 3307）
- 确认 `sys_user` 表里有 admin 和 user
- 删库重建，让 DataInitializer 重新初始化：

```bash
mysql -h 127.0.0.1 -P 3307 -u root -p123456 -e "DROP DATABASE shopping; CREATE DATABASE shopping CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;"
```

然后重启后端。

### Q5: Docker 后端启动失败

查看日志：
```bash
docker compose logs -f backend
```

常见原因：
- MySQL 还没就绪 → 等几秒自动重试，或手动 `docker compose restart backend`
- docker profile 配置不对 → 确认 `application.yml` 里有 docker profile，host 是 `mysql`

### Q6: npm install 慢

```bash
npm config set registry https://registry.npmmirror.com
```

### Q7: Maven 下载依赖慢

项目有 `maven-settings.xml`，用 `-s ../maven-settings.xml` 参数即可。

### Q8: 端口占用（8080 / 3306 / 80）

- **8080 被占**：可能是 Multisim 等软件，改后端端口或杀掉占用进程
- **3306 被占**：Windows 本地 MySQL 服务，Docker MySQL 映射到 3307 避开冲突
- **80 端口被占**：可能是 IIS 或其他 Web 服务，改 docker-compose.yml 里的前端端口映射

---

## 七、项目信息速查

### 地址速查

| 服务 | 地址 | 备注 |
|------|------|------|
| 前端（Vite 开发） | `http://localhost:5173` | 日常开发用，有热更新 |
| 前端（Docker 生产） | `http://localhost` | Docker 全套部署时用 |
| 后端 API | `localhost:8080` | 本地开发时 |
| 后端 API（Docker） | `localhost:8081` | Docker 部署时 |
| Docker MySQL | `localhost:3307` | Navicat / 后端连接 |

### 账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | 123456 |
| 普通用户 | user | 123456 |
| MySQL root | root | 123456 |

### 数据库表结构

共 6 张表：
- `sys_user` — 用户表
- `category` — 商品分类表
- `product` — 商品表
- `cart_item` — 购物车表
- `orders` — 订单表
- `order_item` — 订单明细表

### 文件位置

| 文件 | 路径 (WSL) |
|------|-----------|
| 后端配置 | `~/Project/shopping-system/backend/src/main/resources/application.yml` |
| 建表脚本 | `~/Project/shopping-system/backend/src/main/resources/schema-mysql.sql` |
| 前端配置 | `~/Project/shopping-system/frontend/vite.config.js` |
| Docker 编排 | `~/Project/shopping-system/docker-compose.yml` |
| 后端 Dockerfile | `~/Project/shopping-system/backend/Dockerfile` |
| 前端 Dockerfile | `~/Project/shopping-system/frontend/Dockerfile` |
| Nginx 配置 | `~/Project/shopping-system/frontend/nginx.conf` |
| Maven 配置 | `~/Project/shopping-system/maven-settings.xml` |
